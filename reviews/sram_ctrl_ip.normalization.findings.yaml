# Normalization review of dlds/sram_ctrl_ip_dld.md against its author source
# dlds/sram_ctrl_ip_dld.src.md. See docs/proposals/review_normalization_stage.md.
#
# An empty findings list is NOT a verdict that this normalization is faithful. It
# records that a reviewer looked and reported nothing, which is a weaker claim.
# The human stamp (check_dld_normalization.py --stamp) remains the judgement that
# the meaning survived.
#
# History: the previous review of this pair raised F1 (misplaced_unplaced) -- the
# Figure 3 reissue requirement was filed under Unplaced Source Content instead of
# Open Items, so it never reached the gaps report. That was fixed rather than
# dismissed; see decisions/sram_ctrl_ip.md. This review is of the corrected
# document, which is why an empty list here is not the same claim as an empty list
# on an unreviewed pair.
#
# Checked in particular: the §6 timing table's per-operation cycle counts against
# which FSM each operation's state belongs to in §4 (Request Accept / Bank
# Scheduler / ECC Scrub) -- the class of error this stage exists to catch. Every
# operation lands under the FSM whose states actually perform it, and all cycle
# counts and ns conversions match the source's own arithmetic. Wait models were
# checked against the blocking language they were derived from in each interface
# section, and all state tables, transitions, and command-effect descriptions were
# compared line by line. No finding survived: nothing was dropped, no delay or
# wait model was attached to the wrong FSM or interface, and no state's recorded
# behaviour changed while its name was kept.
ip: sram_ctrl_ip
kind: normalization
source_sha256: 679ca42b42807f89e9c160f21c7600cae9d1d2d213ae2b2587d9664d3c5ba64d
normalized_sha256: 836fc9480a13e2f8b6727f114cd60070bac473b08e879292e8b96ea0b414d233
findings: []
