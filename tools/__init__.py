"""Deterministic gates, generators, and reports for the IP-model pipeline.

Every module here is independently runnable as ``python tools/x.py`` (the
local-clone workflow) and, once this project is pip-installed, importable as
``ip_model_automation.tools.x`` and reachable through the ``ipmodel``
console script (see ``tools/cli.py``).
"""
